import XCTest
@testable import tf_ios_app_flows

final class tf_ios_app_flowsTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(tf_ios_app_flows().text, "Hello, World!")
    }
}
